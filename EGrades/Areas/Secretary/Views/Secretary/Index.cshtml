@model IEnumerable<EGrades.Models.Secretary>
@{
    ViewData["Title"] = "Λίστα Γραμματέων";
}

<h1>Λίστα Γραμματέων</h1>

<hr />

<div class="table-responsive">
    <table id="secretaries-list-table" class="table table-bordered">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(s => s.RegistrationNumber)</th>
                <th>@Html.DisplayNameFor(s => s.FullName)</th>
                <th>Ενέργειες</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.RegistrationNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FullName)
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.Id">Λεπτομέριες</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <strong>Επιτυχία!</strong> @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <strong>Σφάλμα!</strong> @TempData["ErrorMessage"]
    </div>
}

<hr />

<a class="btn btn-primary" href="javascript:history.go(-1)" role="button">Πίσω</a>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ListLayout");

        <script>
            $(document).ready(function () {
                $('#secretaries-list-table').DataTable({
                    "language": {
                        "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Greek.json"
                    }
                });
            });
        </script>
    }
}